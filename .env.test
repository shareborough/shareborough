# Test environment configuration
# Copy this to .env.test.local and add your actual values

# Mailpail config for email E2E testing (AWS SES + S3)
# See: https://github.com/AllyourbaseHQ/mailpail
MAILPAIL_DOMAIN=test.shareborough.com
MAILPAIL_BUCKET=ayb-ci-artifacts
MAILPAIL_PREFIX=e2e-emails/
MAILPAIL_REGION=us-east-1

# AYB API URL for E2E tests (local dev server)
VITE_AYB_URL=http://localhost:8090
